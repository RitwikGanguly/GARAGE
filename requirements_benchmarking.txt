# Benchmarking dependencies  (Python ≥ 3.10)
#
# Additional packages needed to run the baseline generative models
# in benchmarking/sota/ and benchmarking/scrna_seq_specific/.
#
# Install:  pip install -r requirements_benchmarking.txt
#
# Note: torch-geometric, torch-scatter, and torch-sparse must be installed
# from the PyG project wheels (see https://pyg.org/whl/).

numpy>=1.26
pandas>=2.2
torch>=2.0
torch-geometric>=2.5
torch-scatter>=2.1
torch-sparse>=0.6
scikit-learn>=1.5
scipy>=1.10
matplotlib>=3.9
