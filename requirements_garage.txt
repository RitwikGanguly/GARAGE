# GARAGE dependencies  (Python ≥ 3.12)
#
# Minimal set required for the core GARAGE pipeline (data_generation/garage.py)
# plus the data-validation scripts (data_validation/).
#
# Install:  pip install -r requirements_garage.txt
#
# For benchmarking (sota + scRNA-seq-specific), also install:
#   pip install -r requirements_benchmarking.txt

numpy>=1.26
pandas>=2.2
torch>=2.0
scikit-learn>=1.5
pyreadr>=0.5
scanpy>=1.9
leidenalg>=0.10
anndata>=0.10
matplotlib>=3.9
seaborn>=0.13
umap-learn>=0.5
POT>=0.9
jupyter>=1.1
