# GARAGE + Benchmarking dependencies  (Python 3.12.5)
#
# Core environment for:
#   - GARAGE pipeline (GAT-based cell selection + GAN-based generation)
#   - Benchmarking baselines (GAN, WGAN, F-GAN, VAE, LSH-GAN, scGAN,
#     scVAE, scDiffusion, GAN-ROS, VAE-ROS)
#   - Ablation studies (leakage fraction, multi-seed synthesis)
#   - Biological analysis (attention-weight enrichment, rare-cell utility)
#
# Conda environment:  ritwik_base
# Install:  pip install -r requirements_garage.txt
#
# For data validation and biological validation, see:
#   requirements_validation.txt  (Python 3.9.25, conda env: scrna)

# ── Deep learning ──
torch==2.7.1
torch-geometric==2.7.0

# ── Core scientific ──
numpy==2.4.4
pandas==2.3.3
scipy==1.17.1
scikit-learn==1.8.0

# ── Visualisation ──
matplotlib==3.10.9
seaborn==0.13.2

# ── Single-cell ──
anndata==0.12.16

# ── Graphs & utilities ──
networkx==3.6.1
